@model string
@{
    ViewContext.Controller.ViewBag.GroupTitle = Html.ResourceText("Admin");
    ViewContext.Controller.ViewBag.Title = Html.ResourceText("DeactivateUserTitle");
}
<h3>
@string.Format(Html.ResourceText("UserDeactivated"), Model)
</h3>
<hr class="space"/>

@Html.ActionLink(Html.ResourceText("ManageUsers"), "Users")
